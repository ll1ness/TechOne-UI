{
  "name": "techone-ui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node build.js && vite build",
    "preview": "vite preview",
    "deploy": "vite build"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "serve": "^14.0.0",
    "vite": "^6.0.0"
  }
}