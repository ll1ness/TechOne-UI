/*! TechOn UI v1.0.0 | MIT License | github.com/techon-ui | Полная сборка без внешних запросов */
(function(global) {
  'use strict';

  // ===== ВСЕ СТИЛИ CSS =====
  const styles = `
    /*! TechOn UI CSS v1.0.0 */
    
    :root {
      --to-primary: #0066ff;
      --to-primary-dark: #0052cc;
      --to-primary-light: #4d94ff;
      --to-success: #10b981;
      --to-warning: #f59e0b;
      --to-danger: #ef4444;
      --to-info: #3b82f6;
      --to-muted: #9ca3af;
      --to-text: #1a1a1a;
      --to-text-muted: #666666;
      --to-text-inverse: #ffffff;
      --to-bg: #ffffff;
      --to-bg-secondary: #f5f5f5;
      --to-bg-tertiary: #f9f9f9;
      --to-border: #e5e5e5;
      --to-radius: 8px;
      --to-radius-sm: 4px;
      --to-radius-lg: 12px;
      --to-radius-xl: 16px;
      --to-radius-full: 999px;
      --to-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      --to-shadow-lg: 0 4px 20px rgba(0, 0, 0, 0.1);
      --to-transition: all 0.2s ease;
    }
    
    [data-theme="dark"] {
      --to-text: #ffffff;
      --to-text-muted: #b0b0b0;
      --to-bg: #1a1a1a;
      --to-bg-secondary: #2d2d2d;
      --to-bg-tertiary: #333333;
      --to-border: #404040;
      --to-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    }

    /* ===== ACCORDION ===== */
    .to-accordion {
      width: 100%;
      border-bottom: 1px solid var(--to-border);
      font-family: system-ui, -apple-system, sans-serif;
    }
    .to-accordion:last-child { border-bottom: none; }
    .to-accordion-header { width: 100%; }
    .to-accordion-trigger {
      width: 100%;
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: none;
      border: none;
      padding: 16px 20px;
      font-size: 16px;
      font-weight: 500;
      color: var(--to-text);
      cursor: pointer;
      text-align: left;
      transition: background-color 0.2s ease;
      border-radius: inherit;
    }
    .to-accordion-trigger:hover { background-color: var(--to-bg-secondary); }
    .to-accordion-trigger:focus-visible {
      outline: none;
      box-shadow: 0 0 0 3px rgba(0, 102, 255, 0.3);
    }
    .to-accordion-icon {
      display: inline-flex;
      transition: transform 0.3s ease;
      font-size: 1.2em;
      line-height: 1;
    }
    .to-accordion[data-open="true"] .to-accordion-icon { transform: rotate(180deg); }
    .to-accordion-content {
      overflow: hidden;
      transition: max-height 0.3s ease, opacity 0.3s ease;
      max-height: 0;
      opacity: 0;
    }
    .to-accordion[data-open="true"] .to-accordion-content {
      max-height: 500px;
      opacity: 1;
    }
    .to-accordion-content-inner {
      padding: 0 20px 20px 20px;
      color: var(--to-text-muted);
      font-size: 16px;
      line-height: 1.6;
    }

    /* ===== BUTTON ===== */
    .to-button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      border: none;
      font-family: system-ui, -apple-system, sans-serif;
      text-decoration: none;
      cursor: pointer;
      transition: var(--to-transition);
      white-space: nowrap;
      line-height: 1;
      border-radius: var(--to-radius);
      font-weight: 500;
      padding: 10px 20px;
      font-size: 16px;
    }
    .to-button:hover { opacity: 0.8; }
    .to-button:focus-visible {
      outline: none;
      box-shadow: 0 0 0 3px rgba(0, 102, 255, 0.3);
    }
    .to-button:disabled { opacity: 0.5; pointer-events: none; }
    .to-button[data-variant="primary"] { background-color: var(--to-primary); color: white; }
    .to-button[data-variant="secondary"] { background-color: var(--to-bg-secondary); color: var(--to-text); }
    .to-button[data-variant="tertiary"] { background-color: transparent; color: var(--to-text); border: 1px solid var(--to-border); }
    .to-button[data-variant="danger"] { background-color: var(--to-danger); color: white; }
    .to-button[data-size="s"] { padding: 8px 16px; font-size: 14px; }
    .to-button[data-size="l"] { padding: 12px 24px; font-size: 18px; }
    .to-button[data-fill-width="true"] { width: 100%; display: flex; }

    /* ===== BADGE ===== */
    .to-badge {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 4px 12px;
      font-size: 12px;
      font-weight: 500;
      line-height: 1;
      border-radius: 20px;
      font-family: system-ui, -apple-system, sans-serif;
      width: fit-content;
    }
    .to-badge[data-variant="neutral"] { background-color: var(--to-bg-secondary); color: var(--to-text); }
    .to-badge[data-variant="success"] { background-color: var(--to-success); color: white; }
    .to-badge[data-variant="warning"] { background-color: var(--to-warning); color: white; }
    .to-badge[data-variant="danger"] { background-color: var(--to-danger); color: white; }
    .to-badge[data-variant="info"] { background-color: var(--to-info); color: white; }

    /* ===== SPINNER ===== */
    .to-spinner {
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }
    .to-spinner::after {
      content: '';
      width: 20px;
      height: 20px;
      border: 2px solid var(--to-border);
      border-top-color: var(--to-primary);
      border-radius: 50%;
      animation: to-spinner-rotate 0.8s linear infinite;
    }
    .to-spinner[data-size="s"]::after { width: 16px; height: 16px; border-width: 2px; }
    .to-spinner[data-size="m"]::after { width: 24px; height: 24px; border-width: 3px; }
    .to-spinner[data-size="l"]::after { width: 32px; height: 32px; border-width: 3px; }
    .to-spinner[data-color="success"]::after { border-top-color: var(--to-success); }
    .to-spinner[data-color="warning"]::after { border-top-color: var(--to-warning); }
    .to-spinner[data-color="danger"]::after { border-top-color: var(--to-danger); }
    @keyframes to-spinner-rotate { to { transform: rotate(360deg); } }

    /* ===== AVATAR ===== */
    .to-avatar {
      position: relative;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
      background-color: var(--to-bg-secondary);
      color: var(--to-text);
      font-weight: 500;
      overflow: hidden;
      flex-shrink: 0;
    }
    .to-avatar[data-size="xs"] { width: 24px; height: 24px; font-size: 10px; }
    .to-avatar[data-size="s"] { width: 32px; height: 32px; font-size: 13px; }
    .to-avatar[data-size="m"] { width: 40px; height: 40px; font-size: 16px; }
    .to-avatar[data-size="l"] { width: 48px; height: 48px; font-size: 19px; }
    .to-avatar[data-size="xl"] { width: 56px; height: 56px; font-size: 22px; }

    /* ===== TAG ===== */
    .to-tag {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      padding: 4px 10px;
      font-size: 12px;
      font-weight: 500;
      border-radius: 16px;
      background-color: var(--to-bg-secondary);
      color: var(--to-text);
    }
    .to-tag[data-variant="primary"] { background-color: var(--to-primary); color: white; }
    .to-tag[data-variant="success"] { background-color: var(--to-success); color: white; }
    .to-tag[data-variant="warning"] { background-color: var(--to-warning); color: white; }
    .to-tag[data-variant="danger"] { background-color: var(--to-danger); color: white; }

    /* ===== CARD ===== */
    .to-card {
      display: flex;
      flex-direction: column;
      background-color: var(--to-bg);
      color: var(--to-text);
      border-radius: var(--to-radius-lg);
      font-family: system-ui, -apple-system, sans-serif;
      width: 100%;
      transition: var(--to-transition);
      border: 1px solid var(--to-border);
      padding: 16px;
    }
    .to-card[data-variant="elevated"] { box-shadow: var(--to-shadow); border: none; }

    /* ===== DROPDOWN ===== */
    .to-dropdown {
      position: relative;
      display: inline-block;
    }
    .to-dropdown-trigger { cursor: pointer; }
    .to-dropdown-menu {
      position: absolute;
      top: 100%;
      left: 0;
      min-width: 200px;
      background-color: var(--to-bg);
      border: 1px solid var(--to-border);
      border-radius: var(--to-radius);
      box-shadow: var(--to-shadow);
      padding: 4px;
      z-index: 1000;
      margin-top: 4px;
      display: none;
    }
    .to-dropdown[data-open="true"] .to-dropdown-menu { display: block; }
    .to-dropdown-item {
      padding: 8px 12px;
      cursor: pointer;
      border-radius: 4px;
      transition: background-color 0.2s;
    }
    .to-dropdown-item:hover { background-color: var(--to-bg-secondary); }

    /* ===== TOOLTIP ===== */
    .to-tooltip {
      position: relative;
      display: inline-block;
    }
    .to-tooltip-trigger { cursor: help; }
    .to-tooltip-content {
      position: absolute;
      background-color: var(--to-text);
      color: var(--to-bg);
      font-size: 12px;
      padding: 4px 8px;
      border-radius: 4px;
      white-space: nowrap;
      z-index: 1000;
      pointer-events: none;
      opacity: 0;
      visibility: hidden;
      transition: opacity 0.2s;
    }
    .to-tooltip:hover .to-tooltip-content {
      opacity: 1;
      visibility: visible;
    }
    .to-tooltip-content[data-position="top"] {
      bottom: 100%;
      left: 50%;
      transform: translateX(-50%);
      margin-bottom: 4px;
    }

    /* ===== PROGRESS ===== */
    .to-progress {
      width: 100%;
    }
    .to-progress-track {
      width: 100%;
      height: 8px;
      background-color: var(--to-bg-secondary);
      border-radius: 4px;
      overflow: hidden;
    }
    .to-progress-fill {
      height: 100%;
      background-color: var(--to-primary);
      border-radius: 4px;
      transition: width 0.3s ease;
    }
    .to-progress-label {
      display: flex;
      justify-content: space-between;
      margin-bottom: 4px;
      font-size: 12px;
    }

    /* ===== BREADCRUMBS ===== */
    .to-breadcrumbs {
      display: flex;
      align-items: center;
      flex-wrap: wrap;
      gap: 8px;
      font-size: 14px;
    }
    .to-breadcrumbs-item {
      color: var(--to-text-muted);
      text-decoration: none;
    }
    .to-breadcrumbs-item[data-active="true"] { color: var(--to-text); font-weight: 500; }
    .to-breadcrumbs-separator { color: var(--to-border); }

    /* ===== ALERT/BANNER ===== */
    .to-banner {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 16px;
      border-radius: var(--to-radius);
      background-color: var(--to-bg-secondary);
      color: var(--to-text);
      border-left: 4px solid var(--to-primary);
    }
    .to-banner[data-variant="success"] { border-left-color: var(--to-success); }
    .to-banner[data-variant="warning"] { border-left-color: var(--to-warning); }
    .to-banner[data-variant="danger"] { border-left-color: var(--to-danger); }

    /* ===== FLEX/GRID ===== */
    .to-flex {
      display: flex;
    }
    .to-flex[data-direction="column"] { flex-direction: column; }
    .to-flex[data-gap="s"] { gap: 8px; }
    .to-flex[data-gap="m"] { gap: 16px; }
    .to-flex[data-gap="l"] { gap: 24px; }
    
    .to-grid {
      display: grid;
      gap: 16px;
    }
    .to-grid[data-columns="2"] { grid-template-columns: repeat(2, 1fr); }
    .to-grid[data-columns="3"] { grid-template-columns: repeat(3, 1fr); }
    .to-grid[data-columns="4"] { grid-template-columns: repeat(4, 1fr); }

    /* ===== TABLE ===== */
    .to-table {
      width: 100%;
      border-collapse: collapse;
    }
    .to-table th {
      background-color: var(--to-bg-secondary);
      padding: 12px;
      text-align: left;
      font-weight: 600;
    }
    .to-table td {
      padding: 12px;
      border-bottom: 1px solid var(--to-border);
    }

    /* ===== TIMELINE ===== */
    .to-timeline {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }
    .to-timeline-item {
      position: relative;
      padding-left: 20px;
    }
    .to-timeline-item::before {
      content: '';
      position: absolute;
      left: 0;
      top: 8px;
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background-color: var(--to-primary);
    }
    .to-timeline-item[data-state="completed"]::before { background-color: var(--to-success); }
    .to-timeline-item[data-state="active"]::before { background-color: var(--to-warning); }

    /* ===== ICON ===== */
    .to-icon {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 20px;
      line-height: 1;
    }

    /* ===== ICON BUTTON ===== */
    .to-icon-button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 36px;
      height: 36px;
      border: none;
      background-color: transparent;
      color: var(--to-text);
      cursor: pointer;
      border-radius: 6px;
      transition: background-color 0.2s;
    }
    .to-icon-button:hover { background-color: var(--to-bg-secondary); }

    /* ===== TOGGLE BUTTON ===== */
    .to-toggle-button {
      padding: 8px 16px;
      border: 1px solid var(--to-border);
      background-color: transparent;
      color: var(--to-text);
      cursor: pointer;
      border-radius: var(--to-radius);
      transition: all 0.2s;
    }
    .to-toggle-button[data-selected="true"] {
      background-color: var(--to-primary);
      color: white;
      border-color: var(--to-primary);
    }

    /* ===== STATUS INDICATOR ===== */
    .to-status-indicator {
      display: inline-flex;
      align-items: center;
      gap: 4px;
    }
    .to-status-indicator::before {
      content: '';
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background-color: var(--to-muted);
    }
    .to-status-indicator[data-color="success"]::before { background-color: var(--to-success); }
    .to-status-indicator[data-color="warning"]::before { background-color: var(--to-warning); }
    .to-status-indicator[data-color="danger"]::before { background-color: var(--to-danger); }

    /* ===== PULSE ===== */
    .to-pulse {
      display: inline-flex;
      position: relative;
    }
    .to-pulse::after {
      content: '';
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: 50%;
      background-color: currentColor;
      opacity: 0.6;
      animation: to-pulse-ring 2s infinite;
    }
    @keyframes to-pulse-ring {
      0% { transform: scale(1); opacity: 0.6; }
      100% { transform: scale(2); opacity: 0; }
    }

    /* ===== SKELETON ===== */
    .to-skeleton {
      display: block;
      background: linear-gradient(90deg, var(--to-bg-secondary) 25%, var(--to-border) 50%, var(--to-bg-secondary) 75%);
      background-size: 200% 100%;
      animation: to-skeleton-shimmer 1.5s infinite;
      border-radius: 4px;
      height: 16px;
    }
    @keyframes to-skeleton-shimmer {
      0% { background-position: 200% 0; }
      100% { background-position: -200% 0; }
    }

    /* ===== SCROLL TOP ===== */
    .to-scroll-top {
      position: fixed;
      bottom: 30px;
      right: 30px;
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background-color: var(--to-primary);
      color: white;
      border: none;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s;
      z-index: 1000;
    }
    .to-scroll-top.visible {
      opacity: 1;
      visibility: visible;
    }

    /* ===== DIALOG ===== */
    .to-dialog-overlay {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-color: rgba(0, 0, 0, 0.5);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 1000;
    }
    .to-dialog {
      background-color: var(--to-bg);
      border-radius: var(--to-radius-lg);
      max-width: 500px;
      width: 90%;
      padding: 24px;
    }
  `;

  // ===== ВСТАВЛЯЕМ СТИЛИ В HEAD =====
  const styleSheet = document.createElement('style');
  styleSheet.textContent = styles;
  document.head.appendChild(styleSheet);

  // ===== TechOnAccordion =====
  class TechOnAccordion {
    constructor(element) {
      this.element = element;
      this.trigger = element.querySelector('.to-accordion-trigger');
      this.content = element.querySelector('.to-accordion-content');
      this.isOpen = element.getAttribute('data-open') === 'true';
      
      this.updateAriaExpanded();
      this.trigger.addEventListener('click', (e) => {
        e.preventDefault();
        this.toggle();
      });
    }

    updateAriaExpanded() {
      this.trigger.setAttribute('aria-expanded', this.isOpen);
    }

    toggle() {
      this.isOpen = !this.isOpen;
      this.element.setAttribute('data-open', this.isOpen);
      this.updateAriaExpanded();
    }

    open() {
      if (!this.isOpen) {
        this.isOpen = true;
        this.element.setAttribute('data-open', true);
        this.updateAriaExpanded();
      }
    }

    close() {
      if (this.isOpen) {
        this.isOpen = false;
        this.element.setAttribute('data-open', false);
        this.updateAriaExpanded();
      }
    }
  }

  // ===== TechOnButton =====
  class TechOnButton {
    constructor(element) {
      this.element = element;
      this.variant = element.dataset.variant || 'primary';
      this.size = element.dataset.size || 'm';
      this.fillWidth = element.dataset.fillWidth === 'true';
      
      this.element.setAttribute('data-variant', this.variant);
      this.element.setAttribute('data-size', this.size);
      this.element.setAttribute('data-fill-width', this.fillWidth);
    }

    setVariant(variant) {
      this.variant = variant;
      this.element.setAttribute('data-variant', variant);
    }

    setSize(size) {
      this.size = size;
      this.element.setAttribute('data-size', size);
    }
  }

  // ===== TechOnBadge =====
  class TechOnBadge {
    constructor(element) {
      this.element = element;
      this.variant = element.dataset.variant || 'neutral';
      this.element.setAttribute('data-variant', this.variant);
    }

    setVariant(variant) {
      this.variant = variant;
      this.element.setAttribute('data-variant', variant);
    }
  }

  // ===== TechOnSpinner =====
  class TechOnSpinner {
    constructor(element) {
      this.element = element;
      this.size = element.dataset.size || 'm';
      this.color = element.dataset.color || 'primary';
      
      this.element.setAttribute('data-size', this.size);
      this.element.setAttribute('data-color', this.color);
    }
  }

  // ===== TechOnAvatar =====
  class TechOnAvatar {
    constructor(element) {
      this.element = element;
      this.size = element.dataset.size || 'm';
      this.element.setAttribute('data-size', this.size);
    }
  }

  // ===== TechOnTag =====
  class TechOnTag {
    constructor(element) {
      this.element = element;
      this.variant = element.dataset.variant || 'default';
      this.element.setAttribute('data-variant', this.variant);
    }
  }

  // ===== TechOnCard =====
  class TechOnCard {
    constructor(element) {
      this.element = element;
      this.variant = element.dataset.variant || 'default';
      this.element.setAttribute('data-variant', this.variant);
    }
  }

  // ===== TechOnDropdown =====
  class TechOnDropdown {
    constructor(element) {
      this.element = element;
      this.trigger = element.querySelector('.to-dropdown-trigger');
      this.menu = element.querySelector('.to-dropdown-menu');
      this.open = element.dataset.open === 'true';
      
      this.element.setAttribute('data-open', this.open);
      
      this.trigger.addEventListener('click', (e) => {
        e.stopPropagation();
        this.toggle();
      });

      document.addEventListener('click', (e) => {
        if (!this.element.contains(e.target)) {
          this.close();
        }
      });
    }

    toggle() {
      this.open = !this.open;
      this.element.setAttribute('data-open', this.open);
    }

    open() {
      this.open = true;
      this.element.setAttribute('data-open', true);
    }

    close() {
      this.open = false;
      this.element.setAttribute('data-open', false);
    }
  }

  // ===== TechOnTooltip =====
  class TechOnTooltip {
    constructor(element) {
      this.element = element;
      this.content = element.querySelector('.to-tooltip-content');
      this.position = element.dataset.position || 'top';
      
      this.content.setAttribute('data-position', this.position);
    }
  }

  // ===== TechOnProgress =====
  class TechOnProgress {
    constructor(element) {
      this.element = element;
      this.value = parseFloat(element.dataset.value) || 0;
      this.max = parseFloat(element.dataset.max) || 100;
      this.fill = element.querySelector('.to-progress-fill');
      this.valueEl = element.querySelector('.to-progress-value');
      
      this.updateProgress();
    }

    updateProgress() {
      if (this.fill) {
        const percentage = (this.value / this.max) * 100;
        this.fill.style.width = `${percentage}%`;
      }
      if (this.valueEl) {
        this.valueEl.textContent = `${Math.round((this.value / this.max) * 100)}%`;
      }
    }

    setValue(value) {
      this.value = Math.min(value, this.max);
      this.updateProgress();
    }
  }

  // ===== TechOnBreadcrumbs =====
  class TechOnBreadcrumbs {
    constructor(element) {
      this.element = element;
      this.items = Array.from(element.querySelectorAll('.to-breadcrumbs-item'));
      this.separator = element.dataset.separator || 'slash';
      
      this.element.setAttribute('data-separator', this.separator);
      
      for (let i = this.items.length - 1; i > 0; i--) {
        const separator = document.createElement('span');
        separator.className = 'to-breadcrumbs-separator';
        separator.textContent = '/';
        this.element.insertBefore(separator, this.items[i]);
      }
    }
  }

  // ===== TechOnBanner =====
  class TechOnBanner {
    constructor(element) {
      this.element = element;
      this.variant = element.dataset.variant || 'info';
      this.element.setAttribute('data-variant', this.variant);
    }
  }

  // ===== TechOnFlex =====
  class TechOnFlex {
    constructor(element) {
      this.element = element;
      this.direction = element.dataset.direction || 'row';
      this.gap = element.dataset.gap || 'm';
      
      this.element.setAttribute('data-direction', this.direction);
      this.element.setAttribute('data-gap', this.gap);
    }
  }

  // ===== TechOnGrid =====
  class TechOnGrid {
    constructor(element) {
      this.element = element;
      this.columns = element.dataset.columns || '2';
      this.gap = element.dataset.gap || 'm';
      
      this.element.setAttribute('data-columns', this.columns);
      this.element.setAttribute('data-gap', this.gap);
    }
  }

  // ===== TechOnTable =====
  class TechOnTable {
    constructor(element) {
      this.element = element;
      this.headers = Array.from(element.querySelectorAll('th[data-sortable="true"]'));
      
      this.headers.forEach(header => {
        header.addEventListener('click', () => this.sort(header));
      });
    }

    sort(header) {
      const index = this.headers.indexOf(header);
      const tbody = this.element.querySelector('tbody');
      const rows = Array.from(tbody.querySelectorAll('tr'));
      
      rows.sort((a, b) => {
        const aVal = a.children[index].textContent.trim();
        const bVal = b.children[index].textContent.trim();
        return aVal.localeCompare(bVal);
      });
      
      tbody.innerHTML = '';
      rows.forEach(row => tbody.appendChild(row));
    }
  }

  // ===== TechOnTimeline =====
  class TechOnTimeline {
    constructor(element) {
      this.element = element;
      this.items = Array.from(element.querySelectorAll('.to-timeline-item'));
    }
  }

  // ===== TechOnIcon =====
  class TechOnIcon {
    constructor(element) {
      this.element = element;
      this.name = element.dataset.name;
      this.size = element.dataset.size || 'm';
      
      this.element.setAttribute('data-size', this.size);
    }
  }

  // ===== TechOnIconButton =====
  class TechOnIconButton {
    constructor(element) {
      this.element = element;
      this.size = element.dataset.size || 'm';
      this.element.setAttribute('data-size', this.size);
    }
  }

  // ===== TechOnToggleButton =====
  class TechOnToggleButton {
    constructor(element) {
      this.element = element;
      this.selected = element.dataset.selected === 'true';
      
      this.element.setAttribute('data-selected', this.selected);
      
      this.element.addEventListener('click', () => {
        this.toggle();
      });
    }

    toggle() {
      this.selected = !this.selected;
      this.element.setAttribute('data-selected', this.selected);
    }
  }

  // ===== TechOnStatusIndicator =====
  class TechOnStatusIndicator {
    constructor(element) {
      this.element = element;
      this.color = element.dataset.color || 'neutral';
      this.element.setAttribute('data-color', this.color);
    }
  }

  // ===== TechOnPulse =====
  class TechOnPulse {
    constructor(element) {
      this.element = element;
      this.color = element.dataset.color || 'primary';
      this.element.setAttribute('data-color', this.color);
    }
  }

  // ===== TechOnSkeleton =====
  class TechOnSkeleton {
    constructor(element) {
      this.element = element;
      this.width = element.dataset.width || '100';
      this.element.style.width = this.width + '%';
    }
  }

  // ===== TechOnScrollTop =====
  class TechOnScrollTop {
    constructor(element) {
      this.element = element;
      this.threshold = parseInt(element.dataset.threshold) || 300;
      
      window.addEventListener('scroll', () => this.checkVisibility());
      this.element.addEventListener('click', () => this.scrollToTop());
    }

    checkVisibility() {
      if (window.pageYOffset > this.threshold) {
        this.element.classList.add('visible');
      } else {
        this.element.classList.remove('visible');
      }
    }

    scrollToTop() {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    }
  }

  // ===== TechOnDialog =====
  class TechOnDialog {
    constructor(element) {
      this.element = element;
      this.overlay = element.querySelector('.to-dialog-overlay');
      this.closeBtn = element.querySelector('.to-dialog-close');
      this.openBtn = document.querySelector(element.dataset.trigger);
      
      this.openBtn?.addEventListener('click', () => this.open());
      this.closeBtn?.addEventListener('click', () => this.close());
      this.overlay?.addEventListener('click', (e) => {
        if (e.target === this.overlay) this.close();
      });
    }

    open() {
      this.element.style.display = 'block';
    }

    close() {
      this.element.style.display = 'none';
    }
  }

  // ===== АВТОМАТИЧЕСКАЯ ИНИЦИАЛИЗАЦИЯ =====
  function initAllComponents() {
    // Accordion
    document.querySelectorAll('.to-accordion').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnAccordion(el);
        el.__to_initialized = true;
      }
    });

    // Button
    document.querySelectorAll('.to-button').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnButton(el);
        el.__to_initialized = true;
      }
    });

    // Badge
    document.querySelectorAll('.to-badge').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnBadge(el);
        el.__to_initialized = true;
      }
    });

    // Spinner
    document.querySelectorAll('.to-spinner').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnSpinner(el);
        el.__to_initialized = true;
      }
    });

    // Avatar
    document.querySelectorAll('.to-avatar').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnAvatar(el);
        el.__to_initialized = true;
      }
    });

    // Tag
    document.querySelectorAll('.to-tag').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnTag(el);
        el.__to_initialized = true;
      }
    });

    // Card
    document.querySelectorAll('.to-card').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnCard(el);
        el.__to_initialized = true;
      }
    });

    // Dropdown
    document.querySelectorAll('.to-dropdown').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnDropdown(el);
        el.__to_initialized = true;
      }
    });

    // Tooltip
    document.querySelectorAll('.to-tooltip').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnTooltip(el);
        el.__to_initialized = true;
      }
    });

    // Progress
    document.querySelectorAll('.to-progress').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnProgress(el);
        el.__to_initialized = true;
      }
    });

    // Breadcrumbs
    document.querySelectorAll('.to-breadcrumbs').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnBreadcrumbs(el);
        el.__to_initialized = true;
      }
    });

    // Banner
    document.querySelectorAll('.to-banner').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnBanner(el);
        el.__to_initialized = true;
      }
    });

    // Flex
    document.querySelectorAll('.to-flex').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnFlex(el);
        el.__to_initialized = true;
      }
    });

    // Grid
    document.querySelectorAll('.to-grid').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnGrid(el);
        el.__to_initialized = true;
      }
    });

    // Table
    document.querySelectorAll('.to-table').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnTable(el);
        el.__to_initialized = true;
      }
    });

    // Timeline
    document.querySelectorAll('.to-timeline').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnTimeline(el);
        el.__to_initialized = true;
      }
    });

    // Icon
    document.querySelectorAll('.to-icon').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnIcon(el);
        el.__to_initialized = true;
      }
    });

    // Icon Button
    document.querySelectorAll('.to-icon-button').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnIconButton(el);
        el.__to_initialized = true;
      }
    });

    // Toggle Button
    document.querySelectorAll('.to-toggle-button').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnToggleButton(el);
        el.__to_initialized = true;
      }
    });

    // Status Indicator
    document.querySelectorAll('.to-status-indicator').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnStatusIndicator(el);
        el.__to_initialized = true;
      }
    });

    // Pulse
    document.querySelectorAll('.to-pulse').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnPulse(el);
        el.__to_initialized = true;
      }
    });

    // Skeleton
    document.querySelectorAll('.to-skeleton').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnSkeleton(el);
        el.__to_initialized = true;
      }
    });

    // Scroll Top
    document.querySelectorAll('.to-scroll-top').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnScrollTop(el);
        el.__to_initialized = true;
      }
    });

    // Dialog
    document.querySelectorAll('.to-dialog-wrapper').forEach(el => {
      if (!el.__to_initialized) {
        new TechOnDialog(el);
        el.__to_initialized = true;
      }
    });
  }

  // ===== ЭКСПОРТ =====
  const TechOnUI = {
    version: '1.0.0',
    
    // Классы компонентов
    Accordion: TechOnAccordion,
    Button: TechOnButton,
    Badge: TechOnBadge,
    Spinner: TechOnSpinner,
    Avatar: TechOnAvatar,
    Tag: TechOnTag,
    Card: TechOnCard,
    Dropdown: TechOnDropdown,
    Tooltip: TechOnTooltip,
    Progress: TechOnProgress,
    Breadcrumbs: TechOnBreadcrumbs,
    Banner: TechOnBanner,
    Flex: TechOnFlex,
    Grid: TechOnGrid,
    Table: TechOnTable,
    Timeline: TechOnTimeline,
    Icon: TechOnIcon,
    IconButton: TechOnIconButton,
    ToggleButton: TechOnToggleButton,
    StatusIndicator: TechOnStatusIndicator,
    Pulse: TechOnPulse,
    Skeleton: TechOnSkeleton,
    ScrollTop: TechOnScrollTop,
    Dialog: TechOnDialog,
    
    // Методы
    init: initAllComponents,
    
    // Для совместимости
    load: (name) => {
      console.log(`TechOn UI: компонент ${name} уже встроен в скрипт`);
      return Promise.resolve();
    },
    loadAll: () => {
      initAllComponents();
      return Promise.resolve();
    }
  };

  // ===== ИНИЦИАЛИЗАЦИЯ ПРИ ЗАГРУЗКЕ =====
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initAllComponents);
  } else {
    setTimeout(initAllComponents, 10);
  }

  // Наблюдаем за новыми элементами
  const observer = new MutationObserver(() => {
    initAllComponents();
  });
  
  if (document.body) {
    observer.observe(document.body, {
      childList: true,
      subtree: true
    });
  }

  // Глобальный объект
  global.TechOnUI = TechOnUI;
  global.to = TechOnUI;

})(typeof window !== 'undefined' ? window : this);