/*! TechOn UI v1.0.0 | MIT License | github.com/techon-ui */
(function(global) {
  'use strict';

  // ===== КОНФИГУРАЦИЯ =====
  const CONFIG = {
    basePath: 'https://raw.githubusercontent.com/ll1ness/techon-ui-modules/com.quanhub.modules/',
    debug: false,
    cache: true,
    version: '1.0.0'
  };

  // ===== СОСТОЯНИЕ =====
  const state = {
    loaded: {
      css: new Set(),
      js: new Set()
    },
    pending: new Map(),
    initialized: new Set()
  };

  // ===== РЕЕСТР КОМПОНЕНТОВ =====
  const COMPONENTS = {
    // Accordion
    accordion: {
      name: 'Accordion',
      css: ['accordeon/techno-accordion.css'],
      js: ['accordeon/techno-accordion.js'],
      selector: '.to-accordion',
      initClass: 'TechOnAccordion'
    },
    'accordion-group': {
      name: 'Accordion Group',
      css: ['accordion-group/techno-accordion-group.css'],
      js: ['accordion-group/techno-accordion-group.js'],
      dependencies: ['accordion'],
      selector: '.to-accordion-group',
      initClass: 'TechOnAccordionGroup'
    },

    // Animation
    animation: {
      name: 'Animation',
      css: ['animation/techno-animation.css'],
      js: ['animation/techno-animation.js'],
      selector: '.to-animation',
      initClass: 'TechOnAnimation'
    },

    // Arrow
    arrow: {
      name: 'Arrow',
      css: ['arrow/techno-arrow.css'],
      js: ['arrow/techno-arrow.js'],
      selector: '.to-arrow',
      initClass: 'TechOnArrow'
    },

    // Auto Scroll
    'auto-scroll': {
      name: 'Auto Scroll',
      css: ['auto-scroll/techno-auto-scroll.css'],
      js: ['auto-scroll/techno-auto-scroll.js'],
      selector: '.to-auto-scroll',
      initClass: 'TechOnAutoScroll'
    },

    // Avatar
    avatar: {
      name: 'Avatar',
      css: ['avatar/techno-avatar.css'],
      js: ['avatar/techno-avatar.js'],
      selector: '.to-avatar',
      initClass: 'TechOnAvatar'
    },
    'avatar-group': {
      name: 'Avatar Group',
      css: ['avatar-group/techno-avatar-group.css'],
      js: ['avatar-group/techno-avatar-group.js'],
      dependencies: ['avatar'],
      selector: '.to-avatar-group',
      initClass: 'TechOnAvatarGroup'
    },

    // Background
    background: {
      name: 'Background',
      css: ['background/techno-background.css'],
      js: ['background/techno-background.js'],
      selector: '.to-background',
      initClass: 'TechOnBackground'
    },

    // Badge
    badge: {
      name: 'Badge',
      css: ['badge/techno-badge.css'],
      js: ['badge/techno-badge.js'],
      selector: '.to-badge',
      initClass: 'TechOnBadge'
    },

    // Banner
    banner: {
      name: 'Banner',
      css: ['banner/techno-banner.css'],
      js: ['banner/techno-banner.js'],
      selector: '.to-banner',
      initClass: 'TechOnBanner'
    },

    // Blockquote
    blockquote: {
      name: 'Blockquote',
      css: ['blockquote/techno-blockquote.css'],
      js: ['blockquote/techno-blockquote.js'],
      selector: '.to-blockquote',
      initClass: 'TechOnBlockquote'
    },

    // Breadcrumbs
    breadcrumbs: {
      name: 'Breadcrumbs',
      css: ['breadcrumps/techno-breadcrumbs.css'],
      js: ['breadcrumps/techno-breadcrumbs.js'],
      selector: '.to-breadcrumbs',
      initClass: 'TechOnBreadcrumbs'
    },

    // Button
    button: {
      name: 'Button',
      css: ['button/techno-button.css'],
      js: ['button/techno-button.js'],
      selector: '.to-button',
      initClass: 'TechOnButton'
    },

    // Card
    card: {
      name: 'Card',
      css: ['card/techno-card.css'],
      js: ['card/techno-card.js'],
      selector: '.to-card',
      initClass: 'TechOnCard'
    },

    // Carousel
    carousel: {
      name: 'Carousel',
      css: ['carousel/techno-carousel.css'],
      js: ['carousel/techno-carousel.js'],
      selector: '.to-carousel',
      initClass: 'TechOnCarousel'
    },

    // Compare Image
    compareimage: {
      name: 'Compare Image',
      css: ['compareimage/techno-compare-image.css'],
      js: ['compareimage/techno-compare-image.js'],
      selector: '.to-compare-image',
      initClass: 'TechOnCompareImage'
    },

    // Context Menu
    contextmenu: {
      name: 'Context Menu',
      css: ['contextmenu/techno-context-menu.css'],
      js: ['contextmenu/techno-context-menu.js'],
      selector: '.to-context-menu',
      initClass: 'TechOnContextMenu'
    },

    // Cursor Card
    cursorcard: {
      name: 'Cursor Card',
      css: ['cursorcard/techno-cursor-card.css'],
      js: ['cursorcard/techno-cursor-card.js'],
      selector: '.to-cursor-card',
      initClass: 'TechOnCursorCard'
    },

    // Dialog
    dialog: {
      name: 'Dialog',
      css: ['dialog/techno-dialog.css'],
      js: ['dialog/techno-dialog.js'],
      selector: '.to-dialog-wrapper',
      initClass: 'TechOnDialog'
    },

    // Dropdown
    dropdown: {
      name: 'Dropdown',
      css: ['dropdown/techno-dropdown.css'],
      js: ['dropdown/techno-dropdown.js'],
      selector: '.to-dropdown',
      initClass: 'TechOnDropdown'
    },

    // Dropdown Wrapper
    dropdownwrapper: {
      name: 'Dropdown Wrapper',
      css: ['dropdownwrapper/techno-dropdown-wrapper.css'],
      js: ['dropdownwrapper/techno-dropdown-wrapper.js'],
      selector: '.to-dropdown-wrapper',
      initClass: 'TechOnDropdownWrapper'
    },

    // Element Type
    elementtype: {
      name: 'Element Type',
      css: ['elementtype/techno-element-type.css'],
      js: ['elementtype/techno-element-type.js'],
      selector: '.to-element-type',
      initClass: 'TechOnElementType'
    },

    // Emoji Picker
    emojipicker: {
      name: 'Emoji Picker',
      css: ['emojipicker/techno-emoji-picker.css'],
      js: ['emojipicker/techno-emoji-picker.js'],
      selector: '.to-emoji-picker',
      initClass: 'TechOnEmojiPicker'
    },

    // Fade
    fade: {
      name: 'Fade',
      css: ['fade/techno-fade.css'],
      js: ['fade/techno-fade.js'],
      selector: '.to-fade',
      initClass: 'TechOnFade'
    },

    // Feedback
    feedback: {
      name: 'Feedback',
      css: ['feedback/techno-feedback.css'],
      js: ['feedback/techno-feedback.js'],
      selector: '.to-feedback-toast, .to-feedback-alert',
      initClass: 'TechOnFeedback'
    },

    // Flex
    flex: {
      name: 'Flex',
      css: ['flex/techno-flex.css'],
      js: ['flex/techno-flex.js'],
      selector: '.to-flex',
      initClass: 'TechOnFlex'
    },

    // Grid
    grid: {
      name: 'Grid',
      css: ['grid/techno-grid.css'],
      js: ['grid/techno-grid.js'],
      selector: '.to-grid',
      initClass: 'TechOnGrid'
    },

    // Heading
    heading: {
      name: 'Heading',
      css: ['heading/techno-heading.css'],
      js: ['heading/techno-heading.js'],
      selector: '.to-heading',
      initClass: 'TechOnHeading'
    },

    // Hover
    hover: {
      name: 'Hover',
      css: ['hover/techno-hover.css'],
      js: ['hover/techno-hover.js'],
      selector: '.to-hover',
      initClass: 'TechOnHover'
    },

    // Hover Card
    hovercard: {
      name: 'Hover Card',
      css: ['hovercard/techno-hover-card.css'],
      js: ['hovercard/techno-hover-card.js'],
      selector: '.to-hover-card',
      initClass: 'TechOnHoverCard'
    },

    // Icon
    icon: {
      name: 'Icon',
      css: ['icon/techno-icon.css'],
      js: ['icon/techno-icon.js'],
      selector: '.to-icon',
      initClass: 'TechOnIcon'
    },

    // Icon Button
    iconbutton: {
      name: 'Icon Button',
      css: ['iconbutton/techno-icon-button.css'],
      js: ['iconbutton/techno-icon-button.js'],
      selector: '.to-icon-button',
      initClass: 'TechOnIconButton'
    },

    // Inline Code
    inlinecode: {
      name: 'Inline Code',
      css: ['inlinecode/techno-inline-code.css'],
      js: ['inlinecode/techno-inline-code.js'],
      selector: '.to-inline-code',
      initClass: 'TechOnInlineCode'
    },

    // Kbd
    kbd: {
      name: 'Kbd',
      css: ['kbd/techno-kbd.css'],
      js: ['kbd/techno-kbd.js'],
      selector: '.to-kbd',
      initClass: 'TechOnKbd'
    },

    // Line
    line: {
      name: 'Line',
      css: ['line/techno-line.css'],
      js: ['line/techno-line.js'],
      selector: '.to-line',
      initClass: 'TechOnLine'
    },

    // List
    list: {
      name: 'List',
      css: ['list/techno-list.css'],
      js: ['list/techno-list.js'],
      selector: '.to-list',
      initClass: 'TechOnList'
    },

    // Logo
    logo: {
      name: 'Logo',
      css: ['logo/techno-logo.css'],
      js: ['logo/techno-logo.js'],
      selector: '.to-logo',
      initClass: 'TechOnLogo'
    },

    // Logo Cloud
    logocloud: {
      name: 'Logo Cloud',
      css: ['logocloud/techno-logo-cloud.css'],
      js: ['logocloud/techno-logo-cloud.js'],
      selector: '.to-logo-cloud',
      initClass: 'TechOnLogoCloud'
    },

    // Mask
    mask: {
      name: 'Mask',
      css: ['mask/techno-mask.css'],
      js: ['mask/techno-mask.js'],
      selector: '.to-mask',
      initClass: 'TechOnMask'
    },

    // Masonry Grid
    masonrygrid: {
      name: 'Masonry Grid',
      css: ['masonrygrid/techno-masonry.css'],
      js: ['masonrygrid/techno-masonry.js'],
      selector: '.to-masonry',
      initClass: 'TechOnMasonry'
    },

    // Media
    media: {
      name: 'Media',
      css: ['media/techno-media.css'],
      js: ['media/techno-media.js'],
      selector: '.to-media',
      initClass: 'TechOnMedia'
    },

    // Nav Icon
    navicon: {
      name: 'Nav Icon',
      css: ['navicon/techno-nav-icon.css'],
      js: ['navicon/techno-nav-icon.js'],
      selector: '.to-nav-icon',
      initClass: 'TechOnNavIcon'
    },

    // OG Card
    ogcard: {
      name: 'OG Card',
      css: ['ogcard/techno-og-card.css'],
      js: ['ogcard/techno-og-card.js'],
      selector: '.to-og-card',
      initClass: 'TechOnOgCard'
    },

    // Option
    option: {
      name: 'Option',
      css: ['option/techno-option.css'],
      js: ['option/techno-option.js'],
      selector: '.to-option',
      initClass: 'TechOnOption'
    },

    // Particle
    particle: {
      name: 'Particle',
      css: ['particle/techno-particle.css'],
      js: ['particle/techno-particle.js'],
      selector: '.to-particle',
      initClass: 'TechOnParticle'
    },

    // Progress Bar
    progressbar: {
      name: 'Progress Bar',
      css: ['progressbar/techno-progress.css'],
      js: ['progressbar/techno-progress.js'],
      selector: '.to-progress',
      initClass: 'TechOnProgress'
    },

    // Pulse
    pulse: {
      name: 'Pulse',
      css: ['pulse/techno-pulse.css'],
      js: ['pulse/techno-pulse.js'],
      selector: '.to-pulse',
      initClass: 'TechOnPulse'
    },

    // Scroller
    scroller: {
      name: 'Scroller',
      css: ['scroller/techno-scroller.css'],
      js: ['scroller/techno-scroller.js'],
      selector: '.to-scroller',
      initClass: 'TechOnScroller'
    },

    // Scroll To Top
    scrolltotop: {
      name: 'Scroll To Top',
      css: ['scrolltotop/techno-scroll-top.css'],
      js: ['scrolltotop/techno-scroll-top.js'],
      selector: '.to-scroll-top',
      initClass: 'TechOnScrollTop'
    },

    // Segmented Control
    segmentedcontrol: {
      name: 'Segmented Control',
      css: ['segmentedcontrol/techno-segmented.css'],
      js: ['segmentedcontrol/techno-segmented.js'],
      selector: '.to-segmented',
      initClass: 'TechOnSegmented'
    },

    // Skeleton
    skeleton: {
      name: 'Skeleton',
      css: ['skeleton/techno-skeleton.css'],
      js: ['skeleton/techno-skeleton.js'],
      selector: '.to-skeleton',
      initClass: 'TechOnSkeleton'
    },

    // Smart Link
    smartlink: {
      name: 'Smart Link',
      css: ['smartlink/techno-smart-link.css'],
      js: ['smartlink/techno-smart-link.js'],
      selector: '.to-smart-link',
      initClass: 'TechOnSmartLink'
    },

    // Spinner
    spinner: {
      name: 'Spinner',
      css: ['spinner/techno-spinner.css'],
      js: ['spinner/techno-spinner.js'],
      selector: '.to-spinner',
      initClass: 'TechOnSpinner'
    },

    // Status Indicator
    statusindicator: {
      name: 'Status Indicator',
      css: ['statusindicator/techno-status-indicator.css'],
      js: ['statusindicator/techno-status-indicator.js'],
      selector: '.to-status-indicator',
      initClass: 'TechOnStatusIndicator'
    },

    // Table
    table: {
      name: 'Table',
      css: ['table/techno-table.css'],
      js: ['table/techno-table.js'],
      selector: '.to-table-wrapper',
      initClass: 'TechOnTable'
    },

    // Tag
    tag: {
      name: 'Tag',
      css: ['tag/techno-tag.css'],
      js: ['tag/techno-tag.js'],
      selector: '.to-tag',
      initClass: 'TechOnTag'
    },

    // Text
    text: {
      name: 'Text',
      css: ['text/techno-text.css'],
      js: ['text/techno-text.js'],
      selector: '.to-text',
      initClass: 'TechOnText'
    },

    // Theme Switcher
    themeswitcher: {
      name: 'Theme Switcher',
      css: ['themeswitcher/techno-theme-switcher.css'],
      js: ['themeswitcher/techno-theme-switcher.js'],
      selector: '.to-theme-switcher',
      initClass: 'TechOnThemeSwitcher'
    },

    // Timeline
    timeline: {
      name: 'Timeline',
      css: ['timeline/techno-timeline.css'],
      js: ['timeline/techno-timeline.js'],
      selector: '.to-timeline',
      initClass: 'TechOnTimeline'
    },

    // Toast
    toast: {
      name: 'Toast',
      css: ['toast/techno-toast.css'],
      js: ['toast/techno-toast.js'],
      selector: '.to-toast-container',
      initClass: 'TechOnToast'
    },

    // Toggle Button
    togglebutton: {
      name: 'Toggle Button',
      css: ['togglebutton/techno-toggle.css'],
      js: ['togglebutton/techno-toggle.js'],
      selector: '.to-toggle-button',
      initClass: 'TechOnToggleButton'
    },

    // Tooltip
    tooltip: {
      name: 'Tooltip',
      css: ['tooltip/techno-tooltip.css'],
      js: ['tooltip/techno-tooltip.js'],
      selector: '.to-tooltip',
      initClass: 'TechOnTooltip'
    }
  };

  // ===== ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ =====
  function log(...args) {
    if (CONFIG.debug) console.log('[TechOn UI]', ...args);
  }

  function error(...args) {
    console.error('[TechOn UI]', ...args);
  }

  function getFullPath(path) {
    // Убираем возможный дубль basePath если путь уже полный
    if (path.startsWith('http')) return path;
    return CONFIG.basePath + path;
  }

  async function loadCSS(url) {
    const fullUrl = getFullPath(url);
    
    if (state.loaded.css.has(fullUrl)) return Promise.resolve();
    if (state.pending.has(fullUrl)) return state.pending.get(fullUrl);

    log('Loading CSS:', fullUrl);

    const promise = new Promise((resolve, reject) => {
      const link = document.createElement('link');
      link.rel = 'stylesheet';
      link.href = fullUrl;
      link.onload = () => {
        state.loaded.css.add(fullUrl);
        state.pending.delete(fullUrl);
        log('CSS loaded:', fullUrl);
        resolve();
      };
      link.onerror = () => {
        state.pending.delete(fullUrl);
        error('Failed to load CSS:', fullUrl);
        reject(new Error(`Failed to load CSS: ${fullUrl}`));
      };
      document.head.appendChild(link);
    });

    state.pending.set(fullUrl, promise);
    return promise;
  }

  async function loadJS(url) {
    const fullUrl = getFullPath(url);
    
    if (state.loaded.js.has(fullUrl)) return Promise.resolve();
    if (state.pending.has(fullUrl)) return state.pending.get(fullUrl);

    log('Loading JS:', fullUrl);

    const promise = new Promise((resolve, reject) => {
      const script = document.createElement('script');
      script.src = fullUrl;
      script.onload = () => {
        state.loaded.js.add(fullUrl);
        state.pending.delete(fullUrl);
        log('JS loaded:', fullUrl);
        resolve();
      };
      script.onerror = () => {
        state.pending.delete(fullUrl);
        error('Failed to load JS:', fullUrl);
        reject(new Error(`Failed to load JS: ${fullUrl}`));
      };
      document.body.appendChild(script);
    });

    state.pending.set(fullUrl, promise);
    return promise;
  }

  async function loadDependencies(componentName) {
    const component = COMPONENTS[componentName];
    if (!component) return;

    log(`Loading dependencies for ${componentName}`);

    if (component.dependencies) {
      for (const dep of component.dependencies) {
        await loadComponent(dep);
      }
    }

    if (component.css) {
      for (const css of component.css) {
        await loadCSS(css);
      }
    }

    if (component.js) {
      for (const js of component.js) {
        await loadJS(js);
      }
    }
  }

  function initializeComponent(componentName) {
    const component = COMPONENTS[componentName];
    if (!component || !component.selector) return;
    if (state.initialized.has(componentName)) return;

    log(`Initializing ${componentName}`);

    const elements = document.querySelectorAll(component.selector);
    if (elements.length === 0) return;

    const ClassName = global[component.initClass];
    if (!ClassName) {
      error(`Class ${component.initClass} not found for ${componentName}`);
      return;
    }

    elements.forEach(el => {
      try {
        if (!el.__techon_initialized) {
          new ClassName(el);
          el.__techon_initialized = true;
        }
      } catch (e) {
        error(`Error initializing ${componentName}:`, e);
      }
    });

    state.initialized.add(componentName);
  }

  async function loadComponent(componentName) {
    const component = COMPONENTS[componentName];
    if (!component) {
      error(`Component "${componentName}" not found`);
      return false;
    }

    try {
      await loadDependencies(componentName);
      initializeComponent(componentName);
      return true;
    } catch (e) {
      error(`Failed to load component "${componentName}":`, e);
      return false;
    }
  }

  function loadAll() {
    log('Loading all components...');
    return Promise.all(Object.keys(COMPONENTS).map(loadComponent));
  }

  function scanAndLoad() {
    const found = new Set();
    
    Object.entries(COMPONENTS).forEach(([name, component]) => {
      if (component.selector && document.querySelector(component.selector)) {
        found.add(name);
      }
    });

    log(`Found ${found.size} components on page`);
    return Promise.all([...found].map(loadComponent));
  }

  function setBasePath(path) {
    CONFIG.basePath = path;
    log(`Base path set to: ${path}`);
  }

  function enableDebug() {
    CONFIG.debug = true;
    log('Debug mode enabled');
  }

  // ===== ИНИЦИАЛИЗАЦИЯ =====
  document.addEventListener('DOMContentLoaded', () => {
    log('TechOn UI initialized');
    log(`Base path: ${CONFIG.basePath}`);
    
    scanAndLoad();

    const observer = new MutationObserver((mutations) => {
      mutations.forEach(mutation => {
        mutation.addedNodes.forEach(node => {
          if (node.nodeType === 1) {
            Object.entries(COMPONENTS).forEach(([name, component]) => {
              if (component.selector && node.matches?.(component.selector)) {
                loadComponent(name);
              }
            });
          }
        });
      });
    });

    observer.observe(document.body, {
      childList: true,
      subtree: true
    });
  });

  // ===== ЭКСПОРТ =====
  const TechOnUI = {
    version: CONFIG.version,
    components: COMPONENTS,
    load: loadComponent,
    loadAll: loadAll,
    scan: scanAndLoad,
    setBasePath: setBasePath,
    enableDebug: enableDebug,
    init: scanAndLoad
  };

  global.TechOnUI = TechOnUI;
  global.to = TechOnUI;

})(typeof window !== 'undefined' ? window : this);